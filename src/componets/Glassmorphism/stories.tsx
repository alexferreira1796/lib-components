import Glassmorphism from ".";

export default {
  title: "Glassmorphism",
  component: Glassmorphism,
  argTypes: {},
};

const Template = () => <Glassmorphism />;

export const Default = Template.bind({});
Default.args = {};
